/// Non-web stub. On web this is replaced by `web_env_web.dart` through the
/// conditional import in `app_update.dart`, so the Dart VM (analyzer, tests)
/// never touches `dart:js_interop` / `package:web`.
String appBaseUrl() => '';

void reloadApp() {}
